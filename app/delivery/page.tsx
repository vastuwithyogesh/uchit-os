import { AccessDeniedPanel } from "@/components/access-denied-panel"; import { DeliveryConsole } from "@/components/delivery-console"; import { SiteHeader } from "@/components/site-header"; import { requirePageAccess } from "@/lib/page-access";
export default async function DeliveryPage() { const access = await requirePageAccess("CONSULTANT"); if (!access.allowed) return <main className="page-shell"><SiteHeader title="Delivery" subtitle="Milestones and follow-up" /><AccessDeniedPanel area="Delivery" requiredRole="CONSULTANT" actorRole={access.actor.role} /></main>; return <main className="page-shell"><SiteHeader title="Delivery" subtitle="Current milestone, owner, and next step" /><DeliveryConsole /></main>; }
