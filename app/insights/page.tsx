import { AccessDeniedPanel } from "@/components/access-denied-panel"; import { OperationsIntelligenceView } from "@/components/operations-intelligence-view"; import { SiteHeader } from "@/components/site-header"; import { requirePageAccess } from "@/lib/page-access"; import { loadStateFromPersistence } from "@/lib/persistence"; import { projectOperatingIntelligence } from "@/lib/operating-intelligence";
export default async function InsightsPage(){const access=await requirePageAccess("ADMIN");if(!access.allowed)return <main className="page-shell"><SiteHeader title="Operations" subtitle="Aggregate workload and release attention"/><AccessDeniedPanel area="Operations intelligence" requiredRole="ADMIN" actorRole={access.actor.role}/></main>;try{const state=await loadStateFromPersistence();const projection=projectOperatingIntelligence(state,access.actor,new Date().toISOString());return <main className="page-shell"><SiteHeader title="Operations" subtitle="Aggregate workload and release attention"/><OperationsIntelligenceView projection={projection}/></main>;}catch{return <main className="page-shell"><SiteHeader title="Operations" subtitle="Aggregate workload and release attention"/><section className="card"><h1>Operations could not be loaded</h1><p>Retry. If this continues, run System Check.</p><a className="button" href="/insights">Retry</a><div role="alert" aria-live="polite">No aggregate result is available.</div></section></main>;}}
