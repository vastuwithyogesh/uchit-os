# Local TEST_ONLY Functional Audit

This matrix records observed browser interaction evidence. Automated tests are supporting evidence only.

| Route / control | Severity found | Reproduction | Correction | Observed retest |
|---|---:|---|---|---|
| Step 01 scorecard projection | Critical | Existing exact Case and Project could display Step 01 blocked from a current advance projection. | Existing bound Case/Project is authoritative; new handoff still requires accepted commercial plus confirmed advance or approved complimentary exception. Shared scorecard/Continue/case-card projection aligned. | Pilot and continuous TEST_ONLY cases no longer show COMPLETE and BLOCKED together. |
| Step 02 `Mark floor ready` / footer | Major | The earlier page exposed no visible Next action and required a reload after save. | Shared Founder footer always renders Previous/current action/Next with inline reason; success refreshes server projection. | Browser: READY → click → COMPLETE; `Next step` enabled immediately and preserved exact Case/Floor IDs. |
| Step 03 completion header | Major | Header counted removed/optional legacy groups and contradicted the ready Save state. | Shared current-field validator drives header, cards, Save readiness and server payload. | Browser: required-only prefill says `All required information is complete`; optional blanks do not block. |
| Step 03 invalid required field | Major | Missing required values lacked obvious correction/focus. | Field-invalid styling, `aria-invalid`, inline message, correction count and first-error focus use the shared validator. | Browser: blank Property status shows exact inline alert and focused field; correction clears the error; Save produces COMPLETE and enables Next. |
| Step 03 context | Major | Legacy selectable Client could change intake inside a Case/Floor workspace. | Case → Project → permanent Client is locked and server-resolved; mismatch fails closed. | Browser shows locked context only; focused exact-context tests pass. |
| Step 04 client import boundary | Critical | A walkthrough fixture imported Node crypto into the client graph and crashed the browser. | Walkthrough fixture construction remains server-only; clients receive serializable projections. | All Founder routes used in this audit load without Node-only browser import errors; boundary tests pass. |
| Step 04 upload/status | Major | Upload/record/lock states and disabled reasons were unclear. | Persistent NOT_SELECTED/SELECTED/UPLOADING/UPLOADED_NOT_RECORDED/RECORDED/FAILED states, file metadata, precise disabled reasons and persistent success. | Browser observed SELECTED → UPLOADED NOT RECORDED → RECORDED; uploaded asset auto-selected. |
| Step 04 validation | Major | Invalid degree/rationale/evidence did not consistently identify recovery. | Exact degree/rationale/evidence validator, inline errors, `aria-invalid`, first-error focus and lock helper. | Browser: 360 rejected with exact `<360` message; 270 plus valid rationale enabled lock; confirmation produced COMPLETE and Next. |
| Step 04/05 duplicate upload | Major | Successful upload retained the browser File object and left Upload enabled, permitting accidental duplicate bytes. | Clear the selected browser File object after the protected asset record is returned; replacement requires a new deliberate selection. | Step 05 browser retest: after upload, Upload is disabled; recording the named version produces RECORDED/COMPLETE/Next. Step 04 source regression added; clean-fixture retest remains in the final pass. |
| Step 06 marked evidence and entrance | Major | Duplicate immutable evidence/marker actions could remain available; invalid coordinates needed exact recovery. | Current-version equality disables duplicate evidence and exact marker replay; coordinate helper and inline validation retained. | Browser: both evidence records lock; 101% shows exact error and disables save; 50/50 saves, COMPLETE/Next, duplicate marker disabled. |
| Step 07 document verification | Major | Exact already-recorded manual document could be submitted again. | Exact document comparison disables replay and names replacement/change recovery; Founder approval remains a separate action. | Browser: verify canonical document, approve exact sheet, Step 07 COMPLETE/Next; reopening shows duplicate Verify disabled. |
| Step 08 immutable evaluation | Major | Existing Utility/Shakti snapshots still exposed runnable duplicate actions. | Existing exact-lineage snapshots disable both actions with immutable/regeneration guidance. | Browser pilot: both snapshot buttons disabled; COMPLETE/Next. Continuous case stops at the exact approved-methodology readiness blocker. |
| Step 10 approved Site Analysis | Major | Approved record reopened as an empty editable form. | Load the recorded values and lock approved fields/actions; show immutable successor guidance. | Browser pilot shows approved values read-only and action disabled. |
| Step 11 approved Post-Site Findings | Critical | CSS positional hiding made the only relevant card disappear. | Removed brittle positional hiding; component conditional rendering selects the active card. Approved record is loaded and locked. | Browser pilot shows exact approved findings read-only and Next enabled. |
| Step 12 confirmed balance | Major | An approved balance record without a separately projected proof asset showed another upload action. | Confirmed payment status is authoritative for the immutable payment record and locks upload/reconciliation actions. | Browser pilot shows Verified/Balance confirmed, no redundant upload, COMPLETE/Next. |
| Step 13 TEST_ONLY projection | Critical | Walkthrough preview exposed fake remedy placement/finalisation controls while methodology was blocked. | Replaced it with a static read-only blocked projection. Production Stage B service remains gated. | Browser: only `Stage B actions unavailable` disabled control; exact `BLOCKED_METHOD_INPUT`; no placement/upload/finalisation/report actions. |
| Steps 14–17 guarded walkthrough | None open | These cannot canonically unlock past blocked Stage B. | Server-only TEST_ONLY projections expose representative read-only content without mutation controls. | Browser routes render; Steps 14–16 remain blocked by Step 13; Step 17 retains delivery-disabled state. |
| Lead profile · Open new case | Major | Empty submission lacked a precise first-field recovery and the short-viewport sheet could hide its action row. | Shared required-field errors, optional bounded floor count, first-error focus, distinct server/duplicate errors and a bounded scrolling sheet. | Browser: empty submit identified name/location only; floor `0` produced one exact error; valid required-only input created an independent prospective project and updated Cases & Projects. |
| Global Case/Floor selector | Critical | Earlier primary navigation could rely on broad bootstrap data or an implicit context. | Narrow `/api/founder/cases`, shared server permission check, explicit no-context state, My/All permitted/Needs action/Recent views, revalidated per-user history and exact Continue routing. | Browser: no-context `/founder/continue` stayed on “Select a case”; search/empty/recent states worked; selecting Ground floor preserved exact IDs. CLIENT reload showed Access restricted; direct case API returned 403. |
| Step 04 regeneration boundary | Major | A successfully recorded replacement and locked successor orientation still left Step 04 trapped in `NEEDS_REGENERATION`. | Step 04 now completes from the exact current locked orientation/evidence; downstream invalidations remain on Step 05 and later. | Browser: replacement file SELECTED → UPLOADED NOT RECORDED → RECORDED; successor lock changed Step 04 to COMPLETE and enabled exact Step 05 navigation. |
| TEST_ONLY Step 13 | Critical | Local walkthrough still exposed an interactive remedy canvas despite `BLOCKED_METHOD_INPUT`. | All walkthrough steps, including Step 13, now use the read-only projection; the real Stage B workspace remains reachable only through normal server gates. | Browser at 390×844: Step 13 shows only methodology block/recovery and a disabled Next action; no placement, upload or finalisation control. |
| CRM / pipeline mobile controls | Major | Filters and row/move/contact actions rendered at 36–42px. | Targeted controls and the brand home action now meet the 44px minimum. | Browser at 390×844: `/crm` and `/lead-pipeline` have no visible interactive target below 43px and no horizontal overflow. |
| Disposable role simulation | Critical | Switching the local demo to CLIENT changed the shell only; server pages and the case API still resolved the default Founder actor. | A Strict local-demo-only role cookie plus demo-role request header now feeds the same server resolver; production identity handling is unchanged. | Browser: CLIENT reload displays the SETTER access-denied panel with no Case data. Header-driven `/api/founder/cases` returns 403. Restoring SUPER_ADMIN reopens the selector. |
| Commercial proposal empty state | Major | The page still required a separate service scope template after Brochure-as-Scope superseded that prerequisite. | Empty/readiness copy and internal count now refer to the exact active approved brochure scope version. | Browser shows the approved brochure prerequisite and explicitly says brochure text never infers pricing. |
| Payment evidence overview | Major | Founder-facing copy described ₹11,000 as a hard minimum and implied that payment was the only case-opening path. | Copy now identifies ₹11,000 as the suggested standard reference and names confirmed agreed advance or the approved complimentary exception. Server payment, exception, case and report gates are unchanged. | Focused commercial/payment tests pass for standard reference, lower/zero special exception, exact zero-value complimentary handoff, owner denial, CAS/idempotency and client-projection privacy. |
| Desktop/mobile route sweep | None open | Primary route and responsive runtime coverage had not been recorded. | No source change required beyond the targeted fixes above. | Browser: all 17 Founder routes, CRM, pipeline, Clients & Cases, Reports, Payment evidence, Media Library, Commercial proposals and Diagnostics rendered without horizontal overflow or console error; guarded public token pages failed closed. |
| Local server handoff | Major | A second dev launch targeted 3020 while an existing vinext instance held the project lock at 3001; browser received connection refused after the first process exited. | Start exactly one disposable walkthrough process and verify the final URL after the latest build. | Browser retest on 2026-08-13: `http://localhost:3020/founder/01?caseId=b297c02c-8e1b-4fa3-81f2-652f2a2786c2&floorId=6da4c48d-9e69-428e-a477-db43de4ad173` renders the exact TEST_ONLY complimentary case at Step 01 COMPLETE with a context-preserving Step 02 link and no console error. |
| Stage B shared placement refactor | Critical | The latest shared tree retained an unreachable pre-refactor placement renderer referencing an undefined `collisionSafeBox`; three UI contracts still inspected the old file after placement and implementation-sheet behavior moved to shared primitives. | Removed only the unreachable duplicate renderer and pointed the contracts at `remediation-workspace-primitives.tsx`. No remedy eligibility, scoring, placement rule, methodology or activation logic changed. A separate Section A visual-fixture property typo was corrected from an undefined shorthand to the already-built `stageBManifest`. | 42 focused Stage B containment/geometry/flow tests, 7 Section A composition tests, TypeScript and the full release gate pass. Browser direct Step 13 access stays blocked; the guarded walkthrough exposes no Stage B mutation path. |
| Case selector keyboard dismissal | Major | On both desktop and mobile, Escape left the case-selection dialog open and did not restore focus to `Case: UV-2026-001`. | Added one shared close path, Escape handling on the dialog, and requestAnimationFrame focus restoration to the trigger. Native search autofocus and ordinary Tab order remain intact; no focus trap was added. | Observed desktop and 390Ã—844 retest: opening focuses `Search cases`; Escape closes the dialog and returns focus to the collapsed case trigger. Focused selector contracts pass. |
| Keyboard traversal evidence | Limitation | The browser-control transport emitted Ctrl+A, Backspace and Escape correctly, but both real Tab/Shift+Tab and native Enter-on-summary attempts retained the same focused node/`BODY`. | Used direct browser focus assertions, real editable-key/Escape events, DOM order, native semantic elements and source/accessibility contracts; did not claim OS-level Tab evidence. | Desktop/mobile observed: sidebar and selector precede the task controls; Step 02 exposes Mark floor ready then Previous/Next; Step 03 Review required fields focuses the invalid Main challenge textarea with `aria-invalid` and exact `aria-describedby`; Step 04 order is file, Upload, uploaded-file select, Record, degree, rationale, Lock, Previous, Next. Disclosure click retains focus on its summary. No horizontal overflow. |
| Remediation visual contract drift | Major | Concurrent remediation styles reintroduced two gradients, breaking the approved restrained visual contract and the latest release gate. | Replaced only those decorative gradients with solid neutral palette colors; no workflow, geometry, methodology or report behavior changed. | The 18 focused brand/Founder visual contracts and the exact full release gate pass on the latest working tree. |

## Open evidence limitation

- True OS-level Tab/Shift+Tab traversal could not be observed because the browser-control transport retained focus on `BODY`; native Enter-on-summary behaved similarly. Ctrl+A, Backspace and Escape were observed as real key events. Equivalent direct focus assertions, DOM order and source/accessibility contracts cover the requested paths, but this is not represented as OS-level Tab evidence.

## Automated release evidence

- `pnpm test:release` passed on 2026-08-13 against the latest shared tree after the mechanical Stage B/Section A corrections, selector focus correction and visual-contract reconciliation.
- The gate includes the 540-test main suite, focused Founder commercial/no-refund/complimentary/statutory/Zoom/pre-staging/communication suites, multi-floor and protected-report pilots, migration/persistence/security checks, TypeScript (`tsc --noEmit`) and the production vinext build.
- Focused evidence after reconciliation: 13 Stage B UI contracts, 11 Stage B geometry/view/vertical-slice tests, 18 Founder flow/reservation containment tests and 7 Section A deterministic composition tests all pass. Stage B remains policy-blocked in the owner walkthrough; no methodology was supplied or inferred.
