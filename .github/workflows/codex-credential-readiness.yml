name: Codex credential readiness

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  credential-readiness:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check Codex credential binding without exposing the secret
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: node .uchit/codex-credential-readiness.mjs
